{
  "name": "sandbox-subgraph",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rimraf build generated",
    "codegen": "graph codegen",
    "build": "graph build",
    "generate": "node scripts/generate-event-names.js",

    "generate:sepolia": "NETWORK=sepolia node scripts/generate-contract-names.js & NETWORK=sepolia node scripts/generate-addresses.js & npm run generate",
    "prepublish:sepolia": "rimraf build generated && graph codegen && npm run generate:sepolia && graph build --network=sepolia",
    "bump:sepolia:patch": "node scripts/bump-version.js sepolia patch",
    "bump:sepolia:minor": "node scripts/bump-version.js sepolia minor",
    "bump:sepolia:major": "node scripts/bump-version.js sepolia major",
    "deploy:sepolia": "graph deploy --version-label $(jq -r '.sepolia' versions.json) sandbox-sepolia",
    "redeploy:sepolia": "NETWORK=sepolia node scripts/retry-deploy.js",
    "publish:sepolia:patch": "npm run prepublish:sepolia && npm run bump:sepolia:patch && npm run redeploy:sepolia",
    "publish:sepolia:minor": "npm run prepublish:sepolia && npm run bump:sepolia:minor && npm run redeploy:sepolia",
    "publish:sepolia:major": "npm run prepublish:sepolia && npm run bump:sepolia:major && npm run redeploy:sepolia",

    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\"",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ sandbox-subgraph",
    "create-local": "graph create --node http://localhost:8020/ sandbox-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ sandbox-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sandbox-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.0"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "ethers": "^6.13.7",
    "matchstick-as": "0.6.0",
    "prettier": "^3.5.2"
  }
}
