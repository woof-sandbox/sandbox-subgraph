type Comet @entity(immutable: false) {
  id: Bytes! # address
  createdAt: BigInt!
}

type User @entity(immutable: false) {
  id: Bytes! # address
  principal: BigInt!
  userAddress: Bytes!
  proxyCometAddress: Bytes!
  createdAt: BigInt!
}
