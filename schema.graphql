type Comet @entity(immutable: false) {
  id: Bytes! # address
  createdAt: BigInt!
}

type Borrower @entity(immutable: false) {
  id: Bytes! # address
  principal: BigInt!
  userAddress: Bytes!
  proxyCometAddress: Bytes!
  createdAt: BigInt!
}
