## Progress
0xc3873dfbdb8d158e6c3bc953f2143c64cb636441
The `Progress` module represents an entity stored in the database, identified by its `id` (defined by the `ProgressId` enum). It stores progress-related or recent values that the system is currently working with. This module is essential for tracking which entities are actively present in the subgraph at any given moment.
